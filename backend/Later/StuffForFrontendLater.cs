namespace backend;

public class StuffForFrontendLater
{
    //LEAVING CODE that is to be tested
    CloudinaryService.CloudinarySignedUrlGenerator generator = new CloudinaryService.CloudinarySignedUrlGenerator(
        "your_cloud_name",
        "your_api_key",
        "your_api_secret"
    );

    //string signedUrl = generator.GenerateAuthenticatedImageUrl("doorbell_images/my_image_id");

    //Console.WriteLine("Signed URL: " + signedUrl);
//END OF CODE that is to be tested
}